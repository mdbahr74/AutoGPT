# Copy this file to ".env" and fill in your Bitunix API keys.
# Read-only keys are enough — this dashboard never places trades.

BITUNIX_API_KEY=
BITUNIX_API_SECRET=

# Port the dashboard runs on (optional, defaults to 5173)
PORT=5173
